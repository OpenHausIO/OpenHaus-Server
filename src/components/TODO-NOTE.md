- compnent.on("ready") -> compnent.once("ready") ändern ?

- Neu initzilaisiern bzw. update funktion besser implementieren (factory()) -> refresh()

- Rename `factory()` zu "init" ?!

- Implement components prefixed with "`!`"